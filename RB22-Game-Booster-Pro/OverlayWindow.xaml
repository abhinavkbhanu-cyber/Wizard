<Window x:Class="RB22.GameBooster.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RB22 Overlay"
        Width="390"
        Height="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Border CornerRadius="22"
            Background="#E80A1020"
            BorderBrush="#557AAFFF"
            BorderThickness="1"
            Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity=".55"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <StackPanel>
                    <TextBlock Text="RB22" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="LIVE PERFORMANCE" FontSize="10" Opacity=".55" Margin="0,1,0,0"/>
                </StackPanel>
                <Button Content="×" DockPanel.Dock="Right" Width="30" Height="30"
                        FontSize="18" Background="#221D2940" BorderThickness="0"
                        Click="Close_Click"/>
            </DockPanel>

            <UniformGrid Grid.Row="1" Columns="2" Margin="0,18,0,14">
                <Border Margin="0,0,7,7" Padding="12" CornerRadius="12" Background="#55151F35">
                    <StackPanel><TextBlock Text="CPU" FontSize="10" Opacity=".55"/><TextBlock x:Name="Cpu" FontSize="22" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Margin="7,0,0,7" Padding="12" CornerRadius="12" Background="#55151F35">
                    <StackPanel><TextBlock Text="RAM" FontSize="10" Opacity=".55"/><TextBlock x:Name="Ram" FontSize="22" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Margin="0,7,7,0" Padding="12" CornerRadius="12" Background="#55151F35">
                    <StackPanel><TextBlock Text="PING" FontSize="10" Opacity=".55"/><TextBlock x:Name="Ping" FontSize="22" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Margin="7,7,0,0" Padding="12" CornerRadius="12" Background="#55151F35">
                    <StackPanel><TextBlock Text="FPS" FontSize="10" Opacity=".55"/><TextBlock Text="N/A" FontSize="22" FontWeight="SemiBold"/></StackPanel>
                </Border>
            </UniformGrid>

            <TextBlock Grid.Row="2" Text="F8  •  press again to hide  •  drag to move"
                       FontSize="10" Opacity=".5" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>